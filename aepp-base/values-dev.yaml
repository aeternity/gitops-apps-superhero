app:
  image:
    tag: "master"
  ingress:
    enabled: true
    annotations:
      traefik.ingress.kubernetes.io/router.entrypoints: websecure
      cert-manager.io/cluster-issuer: letsencrypt
      solver: "route53"
    hosts:
      - host: "base.dev.service.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
    tls:
      - hosts:
        - "base.dev.service.aepps.com"
        secretName: aepp-base-cert
